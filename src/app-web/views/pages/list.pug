html(lang="en")
  include ../includes/head.pug
  body
    include ../includes/header.pug

    main(role="main" class="container")
      div(class="row")
        div(class="col-sm-8 offset-sm-2 blog-main")
          unless pages.length
            p Empty
          each page in pages
            include ../includes/page.pug
            br

    include ../includes/footer.pug
